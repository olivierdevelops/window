# hello.cs — the smallest CapyScript program.
# `log` compiles to console.log and is mirrored onto the window's console.
log "Hello from CapyScript!"
log "This .cs file compiled to JavaScript and runs in a native window."

const name = "capy"
log "powered by " + name
